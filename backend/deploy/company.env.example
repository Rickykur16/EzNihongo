# Add only after review. All missing/other values keep the feature disabled.
COMPANY_WORKSPACE_ENABLED=false
COMPANY_STAFF_ENABLED=false
COMPANY_WORKER_ENABLED=false
COMPANY_INSIGHTS_ENABLED=false
# Existing DATABASE_URL/login credentials are reused by the API. Do not copy
# production secrets to a test environment. The separate migration command
# requires an explicitly supplied COMPANY_DATABASE_URL; no fallback is used.
