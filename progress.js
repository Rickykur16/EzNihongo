(()=>{const app=document.getElementById('progress-app'),labels={kana:'Kana',vocabulary:'Kosakata',kanji:'Kanji',grammar:'Grammar'};const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));async function api(path){const r=await ezApi(path),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'request_failed');return d}function url(path,course){return`${path}?course=${encodeURIComponent(course)}`}function mastery(k,v){const p=v?.percentage;return`<div class="mastery-row"><strong>${labels[k]}</strong><div class="bar"><i style="width:${p==null?0:p}%"></i></div><span class="state">${p==null?'Belum cukup data':p+'% · '}${esc(v?.label||'Belum cukup data')}</span></div>`}function chapter(x){const p=x.performance||{};return`<article class="chapter"><div class="chapter-top"><div><h3>${esc(x.title)}</h3><span class="muted">${esc(x.section||'Kurikulum')}</span></div><strong>${x.progress.percentage}% selesai</strong></div><div class="chapter-meta"><span class="pill">${x.progress.completedLessons}/${x.progress.totalLessons} Pelajaran</span><span class="pill">${p.percentage==null?'Belum cukup data':esc(p.label)+' · '+p.percentage+'%'}</span>${x.reviewDue?`<a class="pill" href="review.html">${x.reviewDue} item review</a>`:''}</div></article>`}function render(d){if(!d.course){app.innerHTML='<div class="page"><div class="empty">Belum ada kelas aktif.</div></div>';return}const c=d.course,a=d.weeklyActivity||{},m=d.mastery||{};const grouped=new Map();for(const x of d.chapters||[]){const key=x.section||'Kurikulum';if(!grouped.has(key))grouped.set(key,[]);grouped.get(key).push(x)}document.getElementById('learn-nav').href=url('welcome.html',c.slug);document.getElementById('live-nav').href=url('live.html',c.slug);app.innerHTML=`<div class="page"><section class="hero"><div><div class="eyebrow">PROGRESS</div><h1>${esc(c.title)}</h1><p class="muted">Course Progress adalah penyelesaian kurikulum; performa berasal dari bukti latihan.</p></div>${d.courses?.length>1?`<select id="course" class="course-select">${d.courses.map(x=>`<option value="${esc(x.slug)}" ${x.id===c.id?'selected':''}>${esc(x.title)}</option>`).join('')}</select>`:''}</section><section class="grid"><article class="card"><div class="eyebrow">COURSE PROGRESS</div><div class="number">${c.progress.percentage}%</div><p class="muted">${c.progress.completedLessons} dari ${c.progress.totalLessons} Pelajaran selesai.</p></article><article class="card"><div class="eyebrow">AKTIVITAS 7 HARI</div><h2>${a.activeDays||0} hari aktif</h2><p class="muted">${a.lessonsCompleted||0} Pelajaran · ${a.reviewQuestions||0} review · ${a.accuracy==null?'Akurasi belum cukup data':a.accuracy+'% akurasi'}</p></article></section><section class="card"><div class="eyebrow">PERFORMA</div><h2>Mastery berdasarkan bukti latihan</h2><div class="mastery">${Object.entries(labels).map(([k])=>mastery(k,m[k])).join('')}</div></section><section class="card" style="margin-top:15px"><div class="eyebrow">AKTIVITAS &amp; TREND</div><p>${esc(d.weeklyInsight?.message||'Belum cukup data untuk melihat tren.')}</p>${d.review?.total?`<a class="btn" href="review.html">Mulai Smart Review (${d.review.total})</a>`:''}</section><section class="sections"><div><div class="eyebrow">PER BAB</div><h2>Progress dan kebutuhan review</h2></div>${[...grouped.entries()].map(([section,items])=>`<section><h3 class="section-title">${esc(section)}</h3><div class="chapters">${items.map(chapter).join('')}</div></section>`).join('')||'<div class="empty">Belum ada Bab pada kelas ini.</div>'}</section></div>`;app.querySelector('#course')?.addEventListener('change',e=>load(e.target.value))}async function load(course=''){try{render(await api('/progress/me'+(course?'?course='+encodeURIComponent(course):'')))}catch(e){app.innerHTML=`<div class="page"><div class="empty">Progress belum bisa dimuat: ${esc(e.message)}</div></div>`}}document.getElementById('logout').onclick=()=>ezLogout();(async()=>{if(await ezRequireAuth('login.html'))load(new URLSearchParams(location.search).get('course')||'')})()})();
